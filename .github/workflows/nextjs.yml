name: Deploy Next.js to Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"

      - name: Install dependencies
        run: npm install

      - name: Build Next.js
        run: npm run build
<<<<<<< HEAD:.github/workflows/deploy.yml
        env:
          NEXT_PUBLIC_BASE_PATH: /Doorly_Dashboard
          NODE_ENV: production
      
      - name: Verify build output
        run: |
          if [ ! -d "out" ]; then
            echo "Error: 'out' directory not found after build"
            ls -la
            exit 1
          fi
          echo "Build output verified:"
          ls -la out | head -20
      
      - name: Setup Pages
        uses: actions/configure-pages@v4
      
=======

>>>>>>> c9f224e11e1e750008f0c770910526ae9f66e3cc:.github/workflows/nextjs.yml
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./out

  deploy:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
